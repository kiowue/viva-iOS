
struct VivaApi {
    static let urlScheme = "https"
    
    static let host = "e74d8912.ngrok.io"
    
    static let homeContent = "\(urlScheme)://\(host)viva/v0/account"
    static let itemCategories = "\(urlScheme)://\(host)/viva/v0/categories"
    static let verifyCartContent = "\(urlScheme)://\(host)/viva/v0/verifyCartContent"
}
